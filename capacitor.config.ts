import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cotizador_asimed',
  appName: 'cotizador_asimed',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
